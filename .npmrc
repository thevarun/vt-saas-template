# pnpm config (migrated from npm). See docs/upstream-sync-guide.md.
# Preserve the permissive peer-dep behaviour npm had under legacy-peer-deps=true.
strict-peer-dependencies=false
auto-install-peers=true
